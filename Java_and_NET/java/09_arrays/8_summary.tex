% TODO proof-reading
\subsectionold{\EN{Summary}\RU{Итоги}}

\EN{Is it possible to do a buffer overflow in Java?}
\RU{Возможно ли сделать переполнение буфера в Java?}
\EN{No, because the array's length is always present in an array object, 
array bounds are controlled, and an exception is to be raised in case of out-of-bounds access.}
\RU{Нет, потому что длина массива всегда присутствует в объекте массива,
границы массива контролируются и при попытке выйти за границы, сработает исключение.}

\EN{There are no multi-dimensional arrays in Java in the \CCpp sense, so Java is not very suited
for fast scientific computations.}
\RU{В Java нет многомерных массивов в том смысле, как в \CCpp, так что Java не очень подходит
для быстрых научных вычислений.}
%\EN{Perhaps it's payback for memory safety, which is absent in \CCpp.}
%\RU{Видимо, это расплата за безопасность работы с памятью, которой нет в \CCpp.}

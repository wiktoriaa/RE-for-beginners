include(`commons.m4')add	eax, ebx	; _RU(`реальный код')_EN(`real code')
mul	ecx		; _RU(`реальный код')_EN(`real code')
add	eax, esi	; opaque predicate. _RU(``вместо ADD тут может быть XOR, AND или SHL, и~т.д.'')_EN(``XOR, AND or SHL, etc, can be here instead of ADD.'')

\section{MIPS}

\lstinputlisting[caption=\Optimizing GCC 4.4.5 (IDA)]{\CURPATH/MIPS_O3_IDA.lst.\LANG}

\RU{Регистры с префиксом S- называются \q{saved temporaries}, так что, значение \$S0 сохраняется 
в локальном стеке и восстанавливается во время выхода.}
\EN{Registers prefixed with S- are also called \q{saved temporaries}, 
so \$S0 value is saved in the local stack
and restored upon finish.}

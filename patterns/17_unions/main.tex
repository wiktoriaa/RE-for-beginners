\chapter{\RU{Объединения (union)}\EN{Unions}}

\EN{\CCpp \IT{unions} are mostly used for interpreting variable (or memory block) of one data type as variable of another data type.}
\RU{\IT{unions} в \CCpp используются в основном для интерпертации переменной (или блока памяти) одного типа как переменной другого типа.}

% sections
\input{patterns/17_unions/FPU_PRNG/main}
\input{patterns/17_unions/epsilon/main}


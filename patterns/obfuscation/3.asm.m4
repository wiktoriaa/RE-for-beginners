include(`commons.m4')add	eax, ebx	; _LANG(`реальный код',`real code')
mul	ecx		; _LANG(`реальный код',`real code')
add	eax, esi	; opaque predicate. _LANG(`вместо ADD тут может быть XOR, AND или SHL, итд.',`XOR, AND or SHL, etc, can be here instead of ADD.')

\subsubsection{(Windows) SEH}
\index{Windows!Structured Exception Handling}

\IFRU{В стеке хранятся записи \ac{SEH} для функции (если они присутствуют)}
{\ac{SEH} records are also stored on the stack (if they present).}.

\IFRU{Читайте больше о нем здесь}{Read more about it}: (\ref{sec:SEH}).


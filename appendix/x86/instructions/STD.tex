\index{x86!\Instructions!STD}
  \item[STD] (M) \RU{установить флаг DF}\EN{set DF flag}
   \RU{Эта инструкция не генерируется компиляторами и вообще редкая.}
   \EN{This instruction is not generated by compilers and generally rare.}
   \RU{Например, она может быть найдена в файле}\EN{For example, it can be found in} 
   \TT{ntoskrnl.exe} \RU{(ядро Windows) в написанных вручную ф-циях копирования 
   памяти}\EN{Windows kernel file only in hand-written memory copy routines}.
